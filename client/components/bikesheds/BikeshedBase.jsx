var React = require('react'),
  {RouteHandler} = require('react-router');

var BikeshedBase = React.createClass({
  render: function () {
    return <RouteHandler/>;
  }
});

module.exports = BikeshedBase;
